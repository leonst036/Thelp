{
  "localhost": {
    "name": "localhost",
    "host": "localhost",
    "port": 22,
    "username": "root",
    "password": "root"
  "commands": {
      "update": {
        "name": "Update System",
        "command": "sudo apt update && sudo apt upgrade -y"
      },
      "status": {
        "name": "Check SSH Status",
        "command": "systemctl status ssh"
      }
    }
  }
}